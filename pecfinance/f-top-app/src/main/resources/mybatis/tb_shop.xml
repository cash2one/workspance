<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.zz91.top.app.persist.TbShopMapper">

	<insert id="insertByTb" parameterType="taobaoShop"
		useGeneratedKeys="true" keyProperty="id">
		insert into tb_shop(`sid`,`cid`,`nick`,`title`,`desc`,`bulletin`,
		`pic_path`,`created`,`modified`,`gmt_created`,`gmt_modified`)
		values (
		#{sid},#{cid},#{nick},#{title},#{desc},#{bulletin},#{picPath},
		#{created},#{modified},now(),now()
		)
	</insert>

	<select id="countShopBySid" parameterType="java.lang.Long" resultType="java.lang.Integer">
		select count(*) as c from tb_shop where sid=#{sid}
	</select>
</mapper>

