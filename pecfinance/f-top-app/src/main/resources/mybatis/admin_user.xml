<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.zz91.top.app.persist.AdminUserMapper">

    <select id="countUser" parameterType="adminUser" resultType="java.lang.Integer" >
    	select count(*) as c from admin_user
    	where username=#{username} and password=#{password} 
    </select>
    
    <update id="updateLastLogin" parameterType="java.lang.String">
    	update admin_user set
			`gmt_last_login`=now(),
			`gmt_modified`=now()
		where `username` = #{username}
    </update>
    
</mapper>

