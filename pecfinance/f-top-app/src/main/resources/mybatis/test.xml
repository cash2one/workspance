<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.zz91.top.app.persist.TestMapper">
<!--	
        <insert id="add" parameterType="Test"
                useGeneratedKeys="true" keyProperty="classId">
                INSERT INTO testtable (NAME,DATE)
                VALUES(#{name},#{birthday})
        </insert>
    -->
    <insert id="insert" parameterType="test" useGeneratedKeys="true" keyProperty="id"> 
       insert into test(col1,col2,col3) values(#{col1},#{col2},#{col3}) 
       <!-- <selectKey keyProperty="id"  order="AFTER" resultType="int">
			select last_insert_id()
		</selectKey> -->
    </insert>
    
    <select id="query" resultType="test" >
    	select id,col1,col2,col3 from test order by id desc limit 20
    </select>
</mapper>

