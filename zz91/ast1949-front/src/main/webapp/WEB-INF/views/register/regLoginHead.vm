#*<script language="javascript">
  <!--
	//jQuery(document).ready(function(){
   	// 	jQuery("#reLogin").click(function login(){
   	// 		var pwd=jQuery("#rePwd").val();
   	// 		var account=jQuery("#reAccount").val();
   	// 		if(account==null||account.length<=0){
   	// 			alert("请输入用户名！");
	//			jQuery("#reAccount").focus();
	//
    //		} else {
    //			if(pwd==null||pwd.length<=0){
    //				alert("请输入密码！");
	//				jQuery("#rePwd").focus();
   	// 			} else {
    //				var data="username="+account+"&password="+pwd;
    //				var url="$!{address.front}/checkuser.htm";
    //				jQuery.ajax({
    //                   type: "POST",
    //                   url: url,
    //                   data: data,
    //				   dataType:"json",
    //                   success: function(msg){
    //                     if(msg!=null){
    //					 	if(msg.success){
    //							location.href='$!{address.myrc}/index.htm'
    //						} else {
    //							alert(msg.data)
    //						}
    //					 }
    //                   }
    //                });
    //			}
    //		}
    //	});
   // });
  //-->
</script>*#

<div id="zz91RegTop">
    <div class="topBanner">
    	<div class="topLogo">
			<a href="$!{address.front}" target="_blank" title="$!{siteInfo.site_name}" class="zz91logoUrl"></a>
			<span class="zz91logoStatus">注册</span>
    	</div>
        <div class="topLogin">
			
			##<div class="loginForm">
				##<label for="reAccount" class="reAccount">用户名:</label>
    			##<input type="text" id="reAccount" class="reAccount" onkeydown='if(event.keyCode==13) jQuery("#reLogin").click()' />
    			##<label for="rePwd" class="rePwd">密码:</label>
    			##<input type="password" id="rePwd" class="rePwd" onkeydown='if(event.keyCode==13) jQuery("#reLogin").click()'/>
    			##<input type="button" id="reLogin" class="buttonMinGray" value="登    录" style="border:none"/>
			##</div>
			
			<div class="loginTip">
                如果您已经是$!{address.name}的会员，您可以在此立即<a href="$!{address.front}/login.htm">登录</a>！
			</div>
		</div>
	</div>
</div>