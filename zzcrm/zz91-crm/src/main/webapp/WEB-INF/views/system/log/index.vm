<script type="text/javascript" src="#springUrl('/app/sale/operationLog.js')" ></script>
<script type="text/javascript" >
	Ext.onReady(function(){
		var grid=new com.zz91.ep.crm.LogGrid({
			id:LOG.LOG_GRID,
			layout:"fit",
			region:"center"
		});
		
		#if($!{id} && $!{id}>0)
			grid.loadLog("$!{id}");
		#end

		var viewport = new Ext.Viewport({
			layout:"border",
			items:[grid]
		});
	})
</script>