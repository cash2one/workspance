<script type="text/javascript" src="#springUrl('/app/system/param.js')" ></script>
<script type="text/javascript">
Ext.onReady(function(){

	var listParam = new com.zz91.crm.param.listParamGrid({
//		layout:"fit",
		id:Const.PARAM_GRID,
		region:"center"
	});

	var listParamType = new com.zz91.crm.param.listParamTypeGrid({
		id:Const.PARAMTYPE_GRID,
		region:"west",
		width:330,
		split:true,
		listeners:{
			"rowclick":function(grid,rowindex,e){
				listParam.getStore().reload({
					params:{types:grid.getStore().getAt(rowindex).get("key")}
				});
			}
		}
	});

	var viewport = new Ext.Viewport({
		layout : "border",
//		border : true,
		items:[listParam,listParamType]
	});


});
</script>