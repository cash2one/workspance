<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="favorite">

    <!-- 
    <typeAlias alias="favorite" type="com.zz91.ep.domain.comp.Favorite" ></typeAlias>    
    
   	<resultMap class="favorite" id="fullResultMap">
    	<result property="id" column="id"/>
    	<result property="uid" column="uid"/>
    	<result property="cid" column="cid"/>
    	<result property="title" column="title"/>
    	<result property="url" column="url"/>
    	<result property="type" column="type"/>
    	<result property="targetId" column="target_id"/>
    	<result property="delStatus" column="del_status"/>
    	<result property="gmtCreated" column="gmt_created"/>
    	<result property="gmtModified" column="gmt_modified"/>
    </resultMap>
    
    <sql id="fullColumn">
    	id,
    	uid,
    	cid,
    	title,
    	url,
    	type,
    	target_id,
    	del_status,
    	gmt_created,
    	gmt_modified
    </sql>
    
    <insert id="createFavorite" parameterClass="favorite">
    	INSERT INTO favorite
			(
			`uid`,
			`cid`,
			`title`,
			`url`,
			`type`,
			`target_id`,
			`gmt_created`,
			`gmt_modified`)
		VALUES
			(#uid#,
			#cid#,
			#title#,
			#url#,
			#type#,
			#targetId#,
			now(),
			now()
			)
     	<selectKey resultClass="java.lang.Integer" keyProperty="id" >
     		select last_insert_id()
     	</selectKey>
    </insert>
    
    <update id="updateFavorite" parameterClass="java.util.Map">
    	update favorite set
    	del_status = #status#,
    	gmt_modified = now()
    	where id=#id# and uid=#uid#
    </update>
    
     <select id="queryFavorites" parameterClass="java.util.Map" resultMap="fullResultMap">
    	select
    	<include refid="fullColumn"/>
    	from favorite
		<include refid="queryFavoritesWhere"/>
		<include refid="common.pageOrderBy"/>
		<include refid="common.pageLimit"/>
    </select>
    
   <select id="queryFavoritesCount" parameterClass="java.util.Map" resultClass="java.lang.Integer">
    	select count(*)
    	from favorite
		<include refid="queryFavoritesWhere"/>
    </select>
    
    <sql id="queryFavoritesWhere">
    	where
    	uid = #uid#
    	and cid = #cid#
    	<isNotEmpty property="type">
    		and type = #type#
    	</isNotEmpty>
    	<isNotEmpty property="delStatus">
    		and del_status = #delStatus#
    	</isNotEmpty>
    </sql>
    -->
    
</sqlMap>