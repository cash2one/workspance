
<script type="text/javascript" >
	Ext.onReady(function(){
		Ext.get("news").on("click",function(){
			Ext.Ajax.request({
			url:Context.ROOT+"/sysconfig/initCacheForNewsCategory.htm",
			success:function(response,opt){
				com.zz91.utils.Msg(MESSAGE.TITLE, MESSAGE.operateSuccess);
			},
			failure:function(response,opt){
				com.zz91.utils.Msg(MESSAGE.TITLE, MESSAGE.submitFailure);
			}
			});
		});
		
		Ext.get("area").on("click",function(){
			Ext.Ajax.request({
			url:Context.ROOT+"/sysconfig/initCacheForAreaCategory.htm",
			success:function(response,opt){
				com.zz91.utils.Msg(MESSAGE.TITLE, MESSAGE.operateSuccess);
			},
			failure:function(response,opt){
				com.zz91.utils.Msg(MESSAGE.TITLE, MESSAGE.submitFailure);
			}
			});
		});
		
		Ext.get("param").on("click",function(){
			Ext.Ajax.request({
			url:Context.ROOT+"/sysconfig/initParam.htm",
			success:function(response,opt){
				com.zz91.utils.Msg(MESSAGE.TITLE, MESSAGE.operateSuccess);
			},
			failure:function(response,opt){
				com.zz91.utils.Msg(MESSAGE.TITLE, MESSAGE.submitFailure);
			}
			});
		});
		
		Ext.get("trade").on("click",function(){
			Ext.Ajax.request({
			url:Context.ROOT+"/sysconfig/initCacheForTradeCategory.htm",
			success:function(response,opt){
				com.zz91.utils.Msg(MESSAGE.TITLE, MESSAGE.operateSuccess);
			},
			failure:function(response,opt){
				com.zz91.utils.Msg(MESSAGE.TITLE, MESSAGE.submitFailure);
			}
			});
		});
		
		
		Ext.get("exhibit").on("click",function(){
			Ext.Ajax.request({
			url:Context.ROOT+"/sysconfig/initCacheForExhibitCategory.htm",
			success:function(response,opt){
				com.zz91.utils.Msg(MESSAGE.TITLE, MESSAGE.operateSuccess);
			},
			failure:function(response,opt){
				com.zz91.utils.Msg(MESSAGE.TITLE, MESSAGE.submitFailure);
			}
			});
		});
		
		Ext.get("exhibitIndustry").on("click",function(){
			Ext.Ajax.request({
			url:Context.ROOT+"/sysconfig/initCacheForExhibitIndustry.htm",
			success:function(response,opt){
				com.zz91.utils.Msg(MESSAGE.TITLE, MESSAGE.operateSuccess);
			},
			failure:function(response,opt){
				com.zz91.utils.Msg(MESSAGE.TITLE, MESSAGE.submitFailure);
			}
			});
		});
		
		Ext.get("subnet").on("click",function(){
			Ext.Ajax.request({
			url:Context.ROOT+"/sysconfig/initCacheForSubnetCategory.htm",
			success:function(response,opt){
				com.zz91.utils.Msg(MESSAGE.TITLE, MESSAGE.operateSuccess);
			},
			failure:function(response,opt){
				com.zz91.utils.Msg(MESSAGE.TITLE, MESSAGE.submitFailure);
			}
			});
		});
	});
	
</script>

<style type="text/css">
	input {height:40px;width:500px;background-color:#99cccc}
</style>
<input type="button" id="news" value="点击重载新闻类别" /><br /><br />
<input type="button" id="param" value="点击重载参数类别" /><br /><br />
<input type="button" id="trade" value="点击重载交易类别" /><br /><br />
<input type="button" id="area" value="点击重载地区类别" /><br /><br />
<input type="button" id="exhibit" value="点击重载展会类别" /><br /><br />
<input type="button" id="exhibitIndustry" value="点击重载展会行业类别" /><br /><br />
<input type="button" id="subnet" value="点击重载子网交易类别" /><br />
